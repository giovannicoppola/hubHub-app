# Screenshots

Generated by `HubHubUITests/ScreenshotTests`, not captured by hand — see
[Screenshots](../../README.md#screenshots) for the three commands.

| File | Screen |
| --- | --- |
| `01-repos.png` | The repository list, with per-metric chips and deltas |
| `02-chart-downloads.png` | Four years of download history for one repo |
| `03-chart-stars.png` | The same repo's stars, showing that the line starts when stars were first tracked |
| `04-search.png` | Searching, with the header counting matches |
| `05-issues.png` | Repos with open issues, most first |
| `06-settings.png` | Source picker, refresh, Alfred import, metric toggles |

**These are public.** `01`, `02`, `04` and `05` are copied into the public
`alfred-hubHub` repo for its README, so they must be shot from a history
containing only public repositories — `/user/repos` lists private ones too, and
their names would otherwise be published along with the counts.
